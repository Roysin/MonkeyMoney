<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/paddingLeft"
    android:paddingRight="@dimen/paddingRight"
    android:paddingBottom="10dp">

    <ListView
        android:id="@+id/msg_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/half_move_button_height"
        android:paddingTop="@dimen/half_move_button_height"
        android:background="@android:color/darker_gray">
    </ListView>

    <ImageView
        android:id="@+id/moveButton"
        android:layout_width="@dimen/move_button_height"
        android:layout_height="@dimen/move_button_height"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:src="@drawable/drag_point"/>

</RelativeLayout>